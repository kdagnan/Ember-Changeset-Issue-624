{{page-title "Changeset Report"}}

<br>
<button type="button" {{on "click" this.onAccessNestedArray}}>Access Nested Array</button>

<h1>Changeset.change keys:</h1>
<ul>
{{#each this.changeKeys as |key|}}
  <li>{{key}}</li>
{{else}}
  Empty
{{/each}}
</ul>

<h1>Changeset.changes keys:</h1>
<ul>
{{#each this.changesObjectsKeys as |key|}}
  <li>{{key}}</li>
{{else}}
  Empty
{{/each}}
</ul>

{{!-- Accessing any nested value on high-level {computers} object causes issue --}}
Computers.PC (Array): {{this.changeset.computers.pc}} 
<br>
Computers.count (Number): {{this.changeset.computers.count}} 
<br>
Computers.favorites.pc (String on Object): {{this.changeset.computers.favorites.pc}} 